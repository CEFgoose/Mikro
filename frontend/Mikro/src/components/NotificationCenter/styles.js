import styled from "styled-components";

export const NotificationCenterWrapper = styled.div`
  position: absolute;
  z-index: 1000;
  top: 15%;
  left: 15px; // = Grid containerPadding => 10px + 5px of desired left margin
`;
